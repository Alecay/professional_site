import { Button } from "react-bootstrap";
import styles from "./ContactButton.module.css";

export default function ContactButton() {
  return <Button className={styles.helloBtn}>Contact Me</Button>;
}
